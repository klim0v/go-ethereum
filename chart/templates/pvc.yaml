apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "tool-node.fullname" . }}-data
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
